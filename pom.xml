<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.mca</groupId>
    <artifactId>mca-automation</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>mca-automation</name>
    <description>Spring Boot service for MCA automation APIs</description>

    <properties>
        <java.version>17</java.version>
        <ocr.tessdataPath>C:/tesseract-5.4.0.20240606/tesseract-5.4.0.20240606/tessdata</ocr.tessdataPath>
        <!-- Blank: the folder above is the Tesseract source tree (no native DLLs), so tess4j loads its
             own bundled Windows natives. Set this only if you install a compiled Tesseract with libtesseract*.dll. -->
        <ocr.nativeLibraryPath></ocr.nativeLibraryPath>
        <mca.captcha.extraction.enabled>true</mca.captcha.extraction.enabled>
        <mca.captcha.ocr.enabled>true</mca.captcha.ocr.enabled>
        <mca.captcha.audit.enabled>false</mca.captcha.audit.enabled>
    </properties>

    <dependencies>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>4.12.0</version>
        </dependency>
        <dependency>
            <groupId>net.sourceforge.tess4j</groupId>
            <artifactId>tess4j</artifactId>
            <version>5.13.0</version>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
    <groupId>org.jsoup</groupId>
    <artifactId>jsoup</artifactId>
    <version>1.17.2</version>
</dependency>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>4.27.0</version>
        </dependency>
        <dependency>
            <groupId>io.github.bonigarcia</groupId>
            <artifactId>webdrivermanager</artifactId>
            <version>5.9.2</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <jvmArguments>-Djava.awt.headless=true --enable-native-access=ALL-UNNAMED "-Djna.library.path=${ocr.nativeLibraryPath}" "-Docr.tessdataPath=${ocr.tessdataPath}" "-Docr.nativeLibraryPath=${ocr.nativeLibraryPath}" -Dmca.captcha.extraction.enabled=${mca.captcha.extraction.enabled} -Dmca.captcha.ocr.enabled=${mca.captcha.ocr.enabled} -Dmca.captcha.audit.enabled=${mca.captcha.audit.enabled}</jvmArguments>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>macos-homebrew-ocr</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <properties>
                <ocr.tessdataPath>/opt/homebrew/share/tessdata</ocr.tessdataPath>
                <ocr.nativeLibraryPath>/opt/homebrew/lib</ocr.nativeLibraryPath>
            </properties>
        </profile>
    </profiles>
</project>
